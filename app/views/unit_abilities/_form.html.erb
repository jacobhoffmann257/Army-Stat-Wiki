<%= form_with(model: unit_ability) do |form| %>
  <% if unit_ability.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(unit_ability.errors.count, "error") %> prohibited this unit_ability from being saved:</h2>

      <ul>
        <% unit_ability.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :unit_id, style: "display: block" %>
    <%= form.number_field :unit_id %>
  </div>

  <div>
    <%= form.label :abilities_id, style: "display: block" %>
    <%= form.number_field :abilities_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
